startCommand:
  type: stdio
  configSchema:
    type: object
    required:
      - ejentumApiKey
    properties:
      ejentumApiKey:
        type: string
        title: Ejentum API Key
        description: Your Ejentum API key. Get one at https://ejentum.com/pricing (30-day free trial; Go or Super tier required for adaptive tools).
      ejentumApiUrl:
        type: string
        title: Ejentum API URL (optional)
        description: Override the default API endpoint. Leave blank to use production.
        default: https://api.ejentum.com/harness/
  commandFunction: |-
    (config) => ({
      command: 'node',
      args: ['dist/index.js'],
      env: {
        EJENTUM_API_KEY: config.ejentumApiKey,
        ...(config.ejentumApiUrl ? { EJENTUM_API_URL: config.ejentumApiUrl } : {})
      }
    })
